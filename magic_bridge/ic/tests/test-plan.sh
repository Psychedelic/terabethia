#!/bin/bash

# tera
# we need to trigger a call on the tera canister
# call the dip20_proxy

# proxy
# check of the corresponding ethereum address has a canister
# if not create one
# or return one

# proxy
# now need to mint the transaction
# and then consume message from tera canister

# proxy
# approve proxy canister for an amount before burn
# call burn with the amount

# tera
# check tera canister has the message