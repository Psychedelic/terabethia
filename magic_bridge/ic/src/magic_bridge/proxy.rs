use ic_kit::{
    candid::{Nat, Principal},
    ic, RejectionCode,
};

pub struct Dip20Proxy {}

impl Dip20Proxy {
    pub async fn mint(
    ) {
    }

    pub async fn burn(
    ) {
    }
}

pub struct Dip721Proxy {}

impl Dip721Proxy {
    pub async fn mint(
    ) {
    }

    pub async fn burn(
    ) {
    }
}